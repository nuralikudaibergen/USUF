enable-pre-post-scripts=true
side-effects-cache=false
# Allow native build scripts for sharp (image optimization) and msw.
# pnpm 11+ reads onlyBuiltDependencies from package.json > pnpm section,
# but for older lockfile versions we keep a fallback here.
ignore-pnpmfile=false
